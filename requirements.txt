# Core Django
Django==5.1.15
django-ninja==1.3.0
djangorestframework==3.15.2
djangorestframework-simplejwt>=5.3.0
drf-spectacular==0.27.2  # OpenAPI/Swagger documentation

# Database
PyMySQL==1.1.1
dj-database-url==2.3.0
cryptography==46.0.7  # Often needed for modern DB connections

# Background Tasks
redis==5.2.0
celery==5.4.0  # Replaces huey for better performance

# Utilities
python-dotenv==1.2.2
pydantic>=2.0.0
Pillow==12.2.0
requests==2.33.0
stripe>=7.0.0
reportlab>=4.0.0  # PDF generation
weasyprint>=60.0  # HTML to PDF generation

# CORS
django-cors-headers==4.6.0

# Social auth (Google, Apple, Facebook)
django-allauth>=0.57

# Static files
whitenoise==6.8.2  # Replaces dj-static/static3 for modern Django

# S3 media storage (activates when AWS_STORAGE_BUCKET_NAME env var is set)
django-storages[s3]==1.14.4

# WSGI/ASGI Server
gunicorn==23.0.0
uvicorn==0.32.1  # Added for ASGI support (Django Ninja/Async)

# Email HTTP backends (Mailgun etc.). Default SMTP backend still works; activate via
# EMAIL_BACKEND=anymail.backends.mailgun.EmailBackend + MAILGUN_API_KEY env vars.
django-anymail[mailgun]==12.0
